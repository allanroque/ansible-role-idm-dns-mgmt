# ansible-role-idm-dns-mgmt
This Ansible role facilitates the management of DNS A (Address) and PTR (Pointer) records in IDM (Identity Management), offering efficient DNS administration capabilities.
